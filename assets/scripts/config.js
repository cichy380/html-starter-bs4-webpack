/**
 * @fileoverview Manages the configuration settings for the plugins
 */

module.exports = Object.freeze({
  // Tooltips
  // https://getbootstrap.com/docs/4.2/components/tooltips/#options
  TOOLTIPS_SETTINGS: {
    // Delay showing and hiding the tooltip (ms)
    delay: {
      show: 500,
      hide: 100
    }
  },
})
